Subject:
  Label_Column: record
  
  Import_Columns:
    - enrollment date
    - medication
    - cohort

Session:
  Label_Column: Imaging session
  
  Import_Columns:
      - Imaging Date
      - Report Sent
      - important date

Acquisition:
  Label_Column: Scan
#    Header: Scan
#    Mapping:
#      ColName1: LabelName1
#      ColName2: LabelName2
#      ColName3: LabelName3
# Mapping/regex may be required for later development.
  
  
  Import_Columns:
      - Note
  
# Parent values MUST be specified.  For example, you cannot provide a "Session" id without a "subject" id.

  

        